// Template: TypeScript service
// Usage: copy this file and replace {{ServiceName}} with your service name.

export interface {{ServiceName}}Port {
  // define interface methods
}

export class {{ServiceName}}Service implements {{ServiceName}}Port {
  // implement methods
}
